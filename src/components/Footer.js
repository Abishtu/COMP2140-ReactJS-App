import React from "react";
import './Footer.css'

/**
 * Simple component to define footer and its styling
 * 
 * @returns {React.JSX.Element} React component of the footer
 */
const Footer = () => {
    return (
        <footer className="Footer">
        </footer>
    );
}

export default Footer;